* reorganize CCS2 to a Class, good support to adjust parameter

* ending condition
  - search all
  - search util xxxx minimize
  - search util no new conformation in xxxx minimize

* ouput mol in runtime

* 改写itcc.Molecular模块
  - Atom类只包含元素类型，不包含坐标(Done)
  - Molecular类管理坐标(Done)
  - read, write模块做对应更改(Done)
  - Molecular类与Scientific.IO.PDB类相互转换
  - (read): rewrite readpdb, readgjf
  - read.readxyz:
   should support more xyz format, and can detect error format.

* itcc.CCS2.shakeH:
  - Add shakeH1, shakeH3

* itcc.Tools.ctools:
  - use C++ to rewrite `torsionangle'

* parallelize

* clean the tinker code

   

